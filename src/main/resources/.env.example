# Required in production — copy to .env and never commit .env
DB_URL=jdbc:postgresql://localhost:5432/tezza_lending
DB_USERNAME=tezza
DB_PASSWORD=change_me_in_production

KAFKA_BOOTSTRAP_SERVERS=localhost:9092

TEZZA_ISSUER_URI=https://api.yourdomain.com
TEZZA_ADMIN_CLIENT_SECRET=change_me_in_production_min_32_chars
TEZZA_MOBILE_CLIENT_SECRET=change_me_in_production_min_32_chars

# Generate with: openssl genrsa 2048 | openssl pkcs8 -topk8 -nocrypt -outform DER | base64 -w0
TEZZA_JWT_PRIVATE_KEY=

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
